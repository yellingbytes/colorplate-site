const translations = {
  zh: {
    pageTitle: "食色录 - 把餐食变成色彩档案",
    metaDescription: "食色录将日常餐食转化为会生长的色彩档案。拍下一份餐盘，生成色彩丝带，持续构建你的饮食色谱。",
    ogDescription: "一座关于可食用色彩记忆的电影感档案。",
    skip: "跳到主要内容",
    brand: "食色录",
    brandHome: "食色录首页",
    sections: "页面分区",
    navSpectrum: "色谱",
    navEat: "饮食",
    navShare: "分享",
    cta: "加入 TestFlight",
    heroKicker: "可食用色彩记忆档案",
    heroTitle: "把餐食变成色彩档案。",
    heroSubhead: "食色录将日常餐食转化为会生长的色彩档案。拍下一份餐盘，生成色彩丝带，持续构建你的饮食色谱。",
    heroVisual: "食色录档案首页预览",
    archiveAlt: "食色录首页档案，展示每日餐食色彩丝带",
    scroll: "继续探索",
    scrollAria: "继续探索",
    conceptLabel: "概念",
    conceptTitle: "每一餐都会留下色彩痕迹。",
    conceptCopy: "大多数食物记录工具计算热量。食色录记住颜色。",
    analysisAria: "食色录餐食分析与食材色彩提取",
    analysisAlt: "食色录餐食分析界面，展示色彩分布、色彩丝带和食材拆解",
    analysisLabel: "餐食分析",
    analysisTitle: "颜色变成记忆。",
    analysisCopy: "一份餐盘会被读成可食用色彩、食材信息，以及加入档案的色彩丝带。",
    ingredientAria: "示例食材",
    ingredientTomato: "番茄",
    ingredientTurmeric: "姜黄",
    ingredientLemon: "柠檬",
    ingredientAvocado: "牛油果",
    ingredientBlueberry: "蓝莓",
    ingredientBlackRice: "黑米",
    spectrumLabel: "色谱",
    spectrumTitle: "看见你真正吃过的颜色。",
    spectrumCopy: "彩虹分数、连续记录、已解锁色彩和饮食色型，会把档案变成一幅安静的可食用色彩肖像。",
    rainbowScore: "彩虹分数",
    colorsDiscovered: "已解锁色彩",
    chromatype: "饮食色型",
    solarForager: "阳光采集者",
    atlasAlt: "食色录色谱图谱，展示已解锁色彩和饮食色型",
    eatLabel: "饮食",
    eatTitle: "发现你缺少的颜色。",
    eatCopy: "食色录帮助你通过食材和菜品探索少吃的颜色，而不是围绕节食或体重做判断。",
    missingAria: "建议补充的缺少色彩",
    missingBlueberry: "蓝莓",
    missingFig: "无花果",
    missingLime: "青柠",
    missingOat: "燕麦",
    eatAlt: "食色录饮食页，展示缺少色彩和菜品灵感",
    dishLabel: "菜品灵感",
    dishTitle: "黑芝麻柑橘沙拉",
    shareLabel: "分享",
    shareTitle: "把一个月变成作品。",
    shareCopy: "导出档案海报、色谱肖像和年度画廊。它们更像展览记录，而不是普通截图。",
    posterAria: "食色录分享海报预览",
    archivePoster: "档案海报",
    junePlates: "6 月 / 51 份餐盘",
    spectrumPortrait: "色谱肖像",
    posterAlt: "食色录 App 生成的分享海报",
    yearGallery: "年度画廊",
    yearAlt: "食色录年度色彩画廊作品",
    privacyLabel: "隐私",
    privacyTitle: "隐私优先。",
    privacyCopy: "无需账号。你的档案只属于你。餐食照片仅用于生成你的食色录分析。",
    privacyPolicy: "隐私政策",
    terms: "使用条款",
    support: "支持",
    appStoreLabel: "App Store 信息",
    appStoreTitle: "清晰、可审核的基础说明。",
    dataPrivacyTitle: "数据与隐私",
    dataPrivacyCopy: "无需账号。餐食照片用于生成分析和档案。位置权限是可选的，只会在附近食物发现功能中使用。",
    aiTitle: "AI 处理",
    aiCopy: "部分分析可能使用安全的第三方 AI 提供方。用户选择照片进行食材和色彩分析时，会明确知道照片正在被处理。",
    regulatoryTitle: "用途说明",
    regulatoryCopy: "食色录是一款可视化食物档案工具，不是医疗设备，也不提供诊断、治疗、饮食处方或临床建议。",
    supportTitle: "支持",
    supportCopy: "你可以在网站和 App 设置中找到支持、隐私请求和法律联系入口。",
    beginLabel: "开始",
    finalTitle: "开始你的色彩档案。",
    footerLine: "把餐食变成会生长的色彩档案。",
    legal: "法律信息",
    privacyShort: "隐私",
    termsShort: "条款",
    languageLabel: "Switch to English",
    languageChip: "EN",
  },
  en: {
    pageTitle: "ColorPlate - Archive the colors you eat",
    metaDescription: "ColorPlate transforms everyday meals into a living color archive. Capture a plate, generate color ribbons, and build your edible spectrum over time.",
    ogDescription: "A cinematic archive for edible color memory.",
    skip: "Skip to content",
    brand: "ColorPlate",
    brandHome: "ColorPlate home",
    sections: "Sections",
    navSpectrum: "Spectrum",
    navEat: "Eat",
    navShare: "Share",
    cta: "Join TestFlight",
    heroKicker: "A visual food archive",
    heroTitle: "Archive the colors you eat.",
    heroSubhead: "ColorPlate turns meal photos into living color ribbons - a visual archive of your food spectrum.",
    heroVisual: "ColorPlate app archive preview",
    archiveAlt: "ColorPlate home archive showing daily meal ribbons",
    scroll: "Scroll to discover",
    scrollAria: "Scroll to discover",
    conceptLabel: "Concept",
    conceptTitle: "Every meal leaves a trace.",
    conceptCopy: "Most food trackers count calories. ColorPlate remembers color.",
    analysisAria: "ColorPlate meal analysis with ingredient color extraction",
    analysisAlt: "ColorPlate analysis screen showing a meal color field, ribbon, and ingredient breakdown",
    analysisLabel: "Analysis",
    analysisTitle: "Color becomes memory.",
    analysisCopy: "A plate resolves into edible colors, ingredient notes, and a ribbon that joins the archive.",
    ingredientAria: "Example extracted ingredients",
    ingredientTomato: "Tomato",
    ingredientTurmeric: "Turmeric",
    ingredientLemon: "Lemon",
    ingredientAvocado: "Avocado",
    ingredientBlueberry: "Blueberry",
    ingredientBlackRice: "Black rice",
    spectrumLabel: "Spectrum",
    spectrumTitle: "See the colors you actually eat.",
    spectrumCopy: "Rainbow Score, streak, discovered colors, and chromatype turn the archive into a quiet portrait of your edible colors.",
    rainbowScore: "Rainbow Score",
    colorsDiscovered: "Colors discovered",
    chromatype: "Chromatype",
    solarForager: "Solar Forager",
    atlasAlt: "ColorPlate spectrum atlas with discovered colors and chromatype",
    eatLabel: "Eat",
    eatTitle: "Discover what's missing.",
    eatCopy: "ColorPlate helps you explore the colors you rarely eat - through ingredients and dishes, not diets.",
    missingAria: "Suggested missing colors",
    missingBlueberry: "Blueberry",
    missingFig: "Fig",
    missingLime: "Lime",
    missingOat: "Oat",
    eatAlt: "ColorPlate Eat view with missing colors and dish inspiration",
    dishLabel: "Dish study",
    dishTitle: "Black sesame citrus salad",
    shareLabel: "Share",
    shareTitle: "Turn your month into artwork.",
    shareCopy: "Export archive posters, spectrum portraits, and year galleries that feel closer to exhibition notes than screenshots.",
    posterAria: "ColorPlate share poster previews",
    archivePoster: "Archive Poster",
    junePlates: "June / 51 plates",
    spectrumPortrait: "Spectrum Portrait",
    posterAlt: "ColorPlate share poster generated by the app",
    yearGallery: "Year Gallery",
    yearAlt: "ColorPlate year gallery artwork",
    privacyLabel: "Privacy",
    privacyTitle: "Private by design.",
    privacyCopy: "No account required. Your archive stays yours. Photos are used to create your ColorPlate analysis.",
    privacyPolicy: "Privacy Policy",
    terms: "Terms of Use",
    support: "Support",
    appStoreLabel: "App Store information",
    appStoreTitle: "Clear, review-ready basics.",
    dataPrivacyTitle: "Data and privacy",
    dataPrivacyCopy: "No account is required. Meal photos are used to create the analysis and archive. Location is optional and used only for nearby food discovery.",
    aiTitle: "AI processing",
    aiCopy: "Some analysis may use secure third-party AI providers. Users are told when a selected photo is processed for color and ingredient analysis.",
    regulatoryTitle: "Regulatory status",
    regulatoryCopy: "ColorPlate is a visual food archive. It is not a medical device and does not provide diagnosis, treatment, dietary, or clinical advice.",
    supportTitle: "Support",
    supportCopy: "Support, privacy requests, and legal contact are available from this site and inside the app settings.",
    beginLabel: "Begin",
    finalTitle: "Begin your color archive.",
    footerLine: "Archive the colors you eat.",
    legal: "Legal",
    privacyShort: "Privacy",
    termsShort: "Terms",
    languageLabel: "切换到中文",
    languageChip: "中",
  },
};

const revealItems = document.querySelectorAll(".reveal");
const parallaxCards = document.querySelectorAll(".parallax-card");
const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
const hero = document.querySelector(".hero");
const heroTitle = document.querySelector("#hero-title");
const heroPhone = document.querySelector(".phone-hero");
const tiltCard = document.querySelector("[data-tilt-card]");
const languageToggle = document.querySelector("[data-language-toggle]");
const languageLabel = document.querySelector("[data-lang-label]");

let currentLanguage = localStorage.getItem("colorplate-language") || "zh";

function getCopy(key) {
  return translations[currentLanguage]?.[key] || translations.zh[key] || translations.en[key] || "";
}

function updateMeta() {
  document.title = getCopy("pageTitle");
  document.querySelector('meta[name="description"]')?.setAttribute("content", getCopy("metaDescription"));
  document.querySelector('meta[property="og:title"]')?.setAttribute("content", getCopy("pageTitle"));
  document.querySelector('meta[property="og:description"]')?.setAttribute("content", getCopy("ogDescription"));
}

function applyTranslatedAttributes(element) {
  const pairs = element.dataset.i18nAttr?.split(",") || [];
  pairs.forEach((pair) => {
    const [attribute, key] = pair.split(":").map((part) => part.trim());
    if (!attribute || !key) return;
    element.setAttribute(attribute, getCopy(key));
  });
}

function splitHeroTitle({ reveal = false } = {}) {
  if (!heroTitle) return;

  const lineSource = currentLanguage === "zh" ? heroTitle.dataset.linesZh : heroTitle.dataset.linesEn;
  const lines = (lineSource || getCopy("heroTitle")).split("|");
  heroTitle.textContent = "";

  lines.forEach((line) => {
    const outer = document.createElement("span");
    const inner = document.createElement("span");
    outer.className = "hero-line";
    inner.textContent = line;
    if (reveal) {
      inner.style.opacity = "1";
      inner.style.filter = "none";
      inner.style.transform = "translateY(0)";
    }
    outer.appendChild(inner);
    heroTitle.appendChild(outer);
  });
  heroTitle.dataset.split = "true";
}

function applyLanguage(language, { revealHero = false } = {}) {
  currentLanguage = language === "en" ? "en" : "zh";
  localStorage.setItem("colorplate-language", currentLanguage);
  document.documentElement.lang = currentLanguage === "zh" ? "zh-Hans" : "en";
  document.body.dataset.language = currentLanguage;

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const key = element.dataset.i18n;
    element.textContent = getCopy(key);
  });
  document.querySelectorAll("[data-i18n-attr]").forEach(applyTranslatedAttributes);

  updateMeta();

  if (languageToggle && languageLabel) {
    languageToggle.setAttribute("aria-label", getCopy("languageLabel"));
    languageToggle.setAttribute("aria-pressed", currentLanguage === "en" ? "true" : "false");
    languageLabel.textContent = getCopy("languageChip");
  }

  splitHeroTitle({ reveal: revealHero });
}

function runHeroIntro() {
  splitHeroTitle();

  const gsapInstance = window.gsap;
  if (reducedMotion || !gsapInstance) {
    if (!reducedMotion) {
      runNativeHeroIntro();
      return;
    }
    document.querySelectorAll(".hero-line span, .hero-kicker, .hero-subhead, .hero .cta-row, .phone-hero, .scroll-cue")
      .forEach((item) => {
        item.style.opacity = "1";
        item.style.filter = "none";
      });
    return;
  }

  const timeline = gsapInstance.timeline({ defaults: { ease: "power3.out" } });

  gsapInstance.set([".hero-kicker", ".hero-line span", ".hero-subhead", ".hero .cta-row", ".phone-hero", ".scroll-cue"], {
    autoAlpha: 0,
  });
  gsapInstance.set(".hero-line span", { yPercent: 110, filter: "blur(16px)" });
  gsapInstance.set(".hero-subhead", { y: 16, filter: "blur(10px)" });
  gsapInstance.set(".hero .cta-row", { y: 14, filter: "blur(8px)" });
  gsapInstance.set(".phone-hero", { "--intro-y": "18px", filter: "blur(8px)" });
  gsapInstance.set(".story-dot", { scale: 0, autoAlpha: 0 });
  gsapInstance.set(".story-line", { scaleX: 0, autoAlpha: 0 });
  gsapInstance.set(".scroll-cue", { "--cue-y": "-8px" });

  timeline
    .to(".story-dot", { scale: 1, autoAlpha: 1, duration: 0.8 }, 0.18)
    .to(".story-line", { scaleX: 1, autoAlpha: 0.62, duration: 1.75 }, 0.68)
    .to(".story-dot", { x: () => Math.min(window.innerWidth * 0.72, 768), autoAlpha: 0.2, duration: 1.7 }, 0.72)
    .to(".story-line", { x: "18vw", autoAlpha: 0.2, duration: 1.7 }, 1.45)
    .to(".hero-kicker", { autoAlpha: 1, duration: 0.7 }, 0.55)
    .to(".hero-line span", { yPercent: 0, autoAlpha: 1, filter: "blur(0px)", duration: 1.05, stagger: 0.16 }, 0.92)
    .to(".hero-subhead", { y: 0, autoAlpha: 1, filter: "blur(0px)", duration: 0.78 }, 1.62)
    .to(".hero .cta-row", { y: 0, autoAlpha: 1, filter: "blur(0px)", duration: 0.74 }, 1.9)
    .to(".phone-hero", { "--intro-y": "0px", autoAlpha: 1, filter: "blur(0px)", duration: 1.12 }, 1.08)
    .to(".story-dot", { autoAlpha: 0, duration: 0.62 }, 2.15)
    .to(".scroll-cue", { "--cue-y": "0px", autoAlpha: 1, duration: 0.7 }, 2.28)
    .to(".story-line", { autoAlpha: 0.08, duration: 1.2 }, 2.4);

  gsapInstance.to(".phone-hero", {
    "--float-y": "-10px",
    duration: 4.8,
    ease: "sine.inOut",
    repeat: -1,
    yoyo: true,
    delay: 2.2,
  });
}

function runNativeHeroIntro() {
  const lineSpans = document.querySelectorAll(".hero-line span");
  const kicker = document.querySelector(".hero-kicker");
  const subhead = document.querySelector(".hero-subhead");
  const ctas = document.querySelector(".hero .cta-row");
  const phone = document.querySelector(".phone-hero");
  const cue = document.querySelector(".scroll-cue");
  const dot = document.querySelector(".story-dot");
  const line = document.querySelector(".story-line");

  [...lineSpans, kicker, subhead, ctas, phone, cue].forEach((item) => {
    if (!item) return;
    item.style.opacity = "0";
  });

  lineSpans.forEach((span, index) => {
    span.animate(
      [
        { transform: "translateY(110%)", opacity: 0, filter: "blur(16px)" },
        { transform: "translateY(0)", opacity: 1, filter: "blur(0px)" },
      ],
      { duration: 1050, delay: 920 + index * 160, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
    );
  });

  kicker?.animate([{ opacity: 0 }, { opacity: 1 }], {
    duration: 700,
    delay: 550,
    easing: "ease-out",
    fill: "forwards",
  });

  subhead?.animate(
    [
      { transform: "translateY(16px)", opacity: 0, filter: "blur(10px)" },
      { transform: "translateY(0)", opacity: 1, filter: "blur(0px)" },
    ],
    { duration: 780, delay: 1620, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
  );

  ctas?.animate(
    [
      { transform: "translateY(14px)", opacity: 0, filter: "blur(8px)" },
      { transform: "translateY(0)", opacity: 1, filter: "blur(0px)" },
    ],
    { duration: 740, delay: 1900, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
  );

  phone?.animate(
    [
      { opacity: 0, filter: "blur(8px)" },
      { opacity: 1, filter: "blur(0px)" },
    ],
    { duration: 1120, delay: 1080, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
  );

  cue?.animate(
    [
      { opacity: 0, transform: "translate(-50%, -8px)" },
      { opacity: 1, transform: "translate(-50%, 0)" },
    ],
    { duration: 700, delay: 2280, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
  );

  dot?.animate(
    [
      { transform: "translateY(-50%) scale(0)", opacity: 0 },
      { transform: "translateY(-50%) scale(1)", opacity: 1, offset: 0.28 },
      { transform: `translate(${Math.min(window.innerWidth * 0.72, 768)}px, -50%) scale(1)`, opacity: 0 },
    ],
    { duration: 2600, delay: 180, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
  );

  line?.animate(
    [
      { transform: "translateY(-50%) scaleX(0)", opacity: 0 },
      { transform: "translateY(-50%) scaleX(1)", opacity: 0.62, offset: 0.5 },
      { transform: "translate(18vw, -50%) scaleX(1)", opacity: 0.08 },
    ],
    { duration: 3600, delay: 680, easing: "cubic-bezier(.22,1,.36,1)", fill: "forwards" }
  );
}

const revealObserver = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
        revealObserver.unobserve(entry.target);
      }
    });
  },
  { rootMargin: "0px 0px -10% 0px", threshold: 0.16 }
);

revealItems.forEach((item) => revealObserver.observe(item));

let ticking = false;

function updateParallax() {
  const midpoint = window.innerHeight / 2;

  parallaxCards.forEach((card) => {
    const rect = card.getBoundingClientRect();
    const distance = (rect.top + rect.height / 2 - midpoint) / midpoint;
    const offset = Math.max(-1, Math.min(1, distance)) * -10;
    card.style.setProperty("--parallax-y", `${offset}px`);
  });

  ticking = false;
}

function requestParallax() {
  if (!ticking) {
    window.requestAnimationFrame(updateParallax);
    ticking = true;
  }
}

if (!reducedMotion) {
  updateParallax();
  window.addEventListener("scroll", requestParallax, { passive: true });
  window.addEventListener("resize", requestParallax);
}

if (!reducedMotion && hero && tiltCard && heroPhone) {
  hero.addEventListener("pointermove", (event) => {
    const rect = hero.getBoundingClientRect();
    const x = (event.clientX - rect.left) / rect.width - 0.5;
    const y = (event.clientY - rect.top) / rect.height - 0.5;
    heroPhone.style.setProperty("--tilt-y", `${x * 7}deg`);
    heroPhone.style.setProperty("--tilt-x", `${y * -5}deg`);
  });

  hero.addEventListener("pointerleave", () => {
    heroPhone.style.setProperty("--tilt-y", "0deg");
    heroPhone.style.setProperty("--tilt-x", "0deg");
  });
}

languageToggle?.addEventListener("click", () => {
  applyLanguage(currentLanguage === "zh" ? "en" : "zh", { revealHero: true });
});

window.addEventListener("DOMContentLoaded", () => {
  applyLanguage(currentLanguage);
  runHeroIntro();
});
